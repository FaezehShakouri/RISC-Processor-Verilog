


// Instantiate the module
IF_ID instance_name (
    .clk(clk), 
    .addr_in(addr_in), 
    .instr_in(instr_in), 
    .hit_fetch_in(hit_fetch_in), 
    .addr_out(addr_out), 
    .instr_out(instr_out), 
    .hit_fetch_out(hit_fetch_out)
    );


